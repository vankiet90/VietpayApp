name: vietpay_app
description: Currency Converter - Vietpay Technical Assessment
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.8.0+1
  flutter_dotenv: ^5.2.1
  json_annotation: ^4.9.0
  cupertino_icons: ^1.0.8
  get_it: ^8.0.3
  logger: ^2.6.0

  drift: ^2.28.0
  sqlite3_flutter_libs: ^0.5.38
  path_provider: ^2.1.5
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.5.4
  json_serializable: ^6.9.5
  drift_dev: ^2.28.1

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - .env